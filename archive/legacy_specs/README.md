# Legacy Specs Archive Index

`archive/legacy_specs/` 现在只保留最小静态参考，不再保留会被误读为当前可执行规范的 legacy 操作手册、项目规格或历史 manifest。

当前保留内容：

- `AI4S_DATABASE_DIAGRAMS.drawio`：历史数据库结构示意图，供追溯参考
- `README.md`：本索引说明

本轮已移除的内容类型：

- 旧 collection / database / failure rules / query dictionary 操作手册
- 旧 runtime 项目规格与 README
- 指向 `data/exports`、`docker compose`、`./ai4s-xhs` 的历史运行说明
- 历史 `paper_llm_manifest.json` 等过程性说明文件

需要理解旧运行脉络时，请优先参考：

- `archive/legacy_collection_runtime/data/db/ai4s_xhs.sqlite3`
- `archive/legacy_collection_runtime/README.md`
- `archive/legacy_collection_runtime/PROVENANCE.md`
